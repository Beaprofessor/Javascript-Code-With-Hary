first.className = "text-black red red-pant  blue"   // yaha par simply apan ne id = first ki class name ko update kara hai .

first.className.add("red")   // simply add karega ye
first.className.remove("red")  // remove 
first.className.toggle("red")  // if it contains red class , otherwise it removes it.
first.className.contains("red")  // it checks whether the class is there or not.